# PR #38 Review Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Review and verify PR #38 (`feat: add --password-store=basic argument to Chromium launch options`) in an isolated worktree.

**Architecture:** Use `git worktree` to create a clean environment. Execute hygiene checks (ruff, pyright) and a full test suite (pytest) including headless E2E verification.

**Tech Stack:** Python 3.11+, uv, Playwright, GitHub CLI.

---

### Task 1: Worktree Initialization

**Files:**
- Create: `worktrees/pr-38`

- [ ] **Step 1: Create the worktree**
Run: `git worktree add worktrees/pr-38 -b fix-chrome-ubuntu-review`
Note: If the branch name already exists locally, use it. If not, fetch it from origin first.

- [ ] **Step 2: Navigate and setup**
Run: `cd worktrees/pr-38`
Run: `uv sync`

- [ ] **Step 3: Verify baseline**
Run: `uv run gflow --help`
Expected: CLI help output.

---

### Task 2: Compliance & Hygiene Check

**Files:**
- Modify: `src/gflow_cli/browser_manager.py` (review only)

- [ ] **Step 1: Verify DCO Sign-off**
Run: `git log -n 2 --show-signature`
Check for `Signed-off-by` in the PR commits.

- [ ] **Step 2: Run Ruff Linting**
Run: `uv run ruff check src tests`
Expected: 0 errors.

- [ ] **Step 3: Run Ruff Formatting Check**
Run: `uv run ruff format --check src tests`
Expected: 0 errors.

- [ ] **Step 4: Run Pyright Type Checking**
Run: `uv run pyright src`
Expected: 0 errors in `src/gflow_cli`.

---

### Task 3: Functional Verification (Unit/Integration)

**Files:**
- Test: `tests/`

- [ ] **Step 1: Run focused auth/browser tests**
Run: `uv run pytest tests/test_browser_manager.py tests/test_auth.py`
Expected: PASS.

- [ ] **Step 2: Run full test suite**
Run: `uv run pytest -q --cov=gflow_cli`
Expected: PASS (excluding live/e2e markers for now).

---

### Task 4: E2E Headless Validation

**Files:**
- Test: `tests/e2e/test_image_batch_e2e.py`

- [ ] **Step 1: Run headless image generation**
Note: This requires a valid profile with session cookies.
Run: `uv run pytest tests/e2e/test_image_batch_e2e.py -m e2e`
Expected: Observe logs for "no_session" or session restoration success.

- [ ] **Step 2: Evaluate reCAPTCHA impact**
Observe if the reCAPTCHA hurdle is triggered and if the session cookies are correctly read from the `--password-store=basic` file.

---

### Task 5: Windows Regression Check

**Files:**
- Modify: `src/gflow_cli/browser_manager.py` (review only)

- [ ] **Step 1: Verify flag on Windows**
Since this task is running on Windows, verify that the browser still launches and correctly uses the profile directory with the added flag.
Run: `uv run python scripts/smoke_image.py` (or a similar script that launches the browser).
Expected: Browser launches and performs action without DPAPI errors.

---

### Task 6: Review Synthesis

- [ ] **Step 1: Draft review comments**
Synthesize findings:
- Did it fix the Linux keyring issue?
- Any regressions on Windows?
- Does it follow project conventions (structlog, async)?
- Is DCO present?

- [ ] **Step 2: Final Recommendation**
Propose to user: Accept, Request Changes, or Reject.
